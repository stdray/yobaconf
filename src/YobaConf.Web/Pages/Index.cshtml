@page
@model YobaConf.Web.Pages.IndexModel
@{
	ViewData["Title"] = "YobaConf";
}

@* v2 skeleton placeholder — real dashboard lands in Phase B.4 (/Bindings). Shown
   post-login so the cookie-redirect target has something to render. *@
<div class="alert" data-testid="index-phase-a-placeholder">
	<div>
		<h2 class="text-lg font-semibold">YobaConf v2 — Phase A (skeleton)</h2>
		<p class="opacity-70 text-sm mt-1">
			Storage and resolve engine are being rebuilt. Admin UI lands in Phase B —
			see <code class="font-mono">doc/plan.md</code>.
		</p>
	</div>
</div>
